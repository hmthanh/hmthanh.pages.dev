<h1>Hello, Svelte (via Bun)</h1>
<p>This example demonstrates <strong>mdsvex</strong> with <strong>Bun</strong>.</p>
<pre
	class="language-js">{@html `<code class="language-js">console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">'Hello from mdsvex and Bun!'</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code>`}</pre>
<h3>4. <strong>Run the Script with Bun</strong></h3>
<p>Use Bun to execute the script:</p>
<pre class="language-bash">{@html `<code class="language-bash">bun compile-mdsvex.js</code>`}</pre>
